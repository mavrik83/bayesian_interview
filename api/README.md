# Express API Bayesian Interview

To get started:

- run `npm install` in the `/api` dir
- delete the `dev.db` and `/migrations` from the `/prisma` directory
- run `npx prisma migrate dev --name init`
- run `npm run dev`
